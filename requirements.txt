# Core dependencies
streamlit>=1.28.0
requests>=2.31.0
python-dotenv>=1.0.0

# LangChain and AI
langchain>=0.1.0
langchain-community>=0.0.10
langchain-core>=0.1.0
langchain_groq>=0.0.1
langchain-huggingface>=0.0.1

# Document processing (lightweight)
pypdf>=3.17.0

# Vector stores
faiss-cpu>=1.7.4

# ML and embeddings
sentence-transformers>=2.2.2

# Data handling
pandas>=2.0.0
openpyxl>=3.1.0

# Note: Removed heavy/unused dependencies for faster deployment:
# - flask (using Streamlit only)
# - unstructured (too large, using pypdf instead)
# - qdrant-client (not used)
# - watchdog (optional)
# - plotly (optional)